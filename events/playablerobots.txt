namespace = playablerobots

### Hidden Game Start Event
pop_event = {
	id = playablerobots.1
	title = playablerobots.2.title
	desc = tutorial.14.desc
	picture = GFX_evt_playablerobots
	hide_window = yes
	#is_triggered_only = yes

	trigger = {
		#is_ai = no
		#planet = {
		#	any_pop = {
		#		AND = {
					has_trait = trait_android
					is_same_species = owner
					AND = {
						NOT = {has_trait = trait_pc_desert_preference}
						NOT = {has_trait = trait_pc_tropical_preference}
						NOT = {has_trait = trait_pc_continental_preference}
						NOT = {has_trait = trait_pc_ocean_preference}
						NOT = {has_trait = trait_pc_arctic_preference}
						NOT = {has_trait = trait_pc_tundra_preference}
						NOT = {has_trait = trait_pc_arid_preference}
						NOT = {has_trait = trait_pc_ai_preference}
						NOT = {has_trait = trait_pc_nuked_preference}
						NOT = {has_trait = trait_pc_gaia_preference}
						NOT = {has_trait = trait_pc_ring_preference}
					}
					#any_country = {
					#	is_ai = no
						#has_trait = trait_pc_desert_preference
					#}
		#		}
		#	}
		#}
	}

	mean_time_to_happen = { days = 1 }

	immediate = {
	}

	option = {
		name = OK
		planet = {
		owner = {
		change_dominant_species = this
		}
		}
		if = {
			limit = {
				#solar_system = {
				#	space_owner = {
				#		is_ai = no
				#		has_trait = trait_agrarian
				#	}
				#}
				planet = {
					exists = owner
					owner = { is_ai = no }
					owner = { has_trait = trait_agrarian }
				}
			}
			modify_species = {
				species = this
				add_trait = trait_agrarian
			}

		}
		if = {
			limit = {
				planet = {
					exists = owner
					owner = { is_ai = no }
					owner = { has_trait = trait_thrifty }
				}
			}
			modify_species = {
				species = this
				add_trait = trait_thrifty
			}

		}
		if = {
			limit = {
				planet = {
					exists = owner
					owner = { is_ai = no }
					owner = { has_trait = trait_industrious }
				}
			}
			modify_species = {
				species = this
				add_trait = trait_industrious
			}

		}
		if = {
			limit = {
				planet = {
					exists = owner
					owner = { is_ai = no }
					owner = { has_trait = trait_intelligent }
				}
			}
			modify_species = {
				species = this
				add_trait = trait_intelligent
			}

		}
		if = {
			limit = {
				planet = {
					exists = owner
					owner = { is_ai = no }
					owner = { has_trait = trait_natural_engineers }
				}
			}
			modify_species = {
				species = this
				add_trait = trait_natural_engineers
			}

		}
		if = {
			limit = {
				planet = {
					exists = owner
					owner = { is_ai = no }
					owner = { has_trait = trait_natural_physicists }
				}
			}
			modify_species = {
				species = this
				add_trait = trait_natural_physicists
			}

		}
		if = {
			limit = {
				planet = {
					exists = owner
					owner = { is_ai = no }
					owner = { has_trait = trait_natural_sociologists }
				}
			}
			modify_species = {
				species = this
				add_trait = trait_natural_sociologists
			}

		}
		if = {
			limit = {
				planet = {
					exists = owner
					owner = { is_ai = no }
					owner = { has_trait = trait_extremely_adaptive }
				}
			}
			modify_species = {
				species = this
				add_trait = trait_extremely_adaptive
			}

		}
		if = {
			limit = {
				planet = {
					exists = owner
					owner = { is_ai = no }
					owner = { has_trait = trait_adaptive }
				}
			}
			modify_species = {
				species = this
				add_trait = trait_adaptive
			}

		}
		if = {
			limit = {
				planet = {
					exists = owner
					owner = { is_ai = no }
					owner = { has_trait = trait_nonadaptive }
				}
			}
			modify_species = {
				species = this
				add_trait = trait_nonadaptive
			}

		}
		if = {
			limit = {
				planet = {
					exists = owner
					owner = { is_ai = no }
					owner = { has_trait = trait_rapid_breeders }
				}
			}
			modify_species = {
				species = this
				add_trait = trait_rapid_breeders
			}

		}
		if = {
			limit = {
				planet = {
					exists = owner
					owner = { is_ai = no }
					owner = { has_trait = trait_slow_breeders }
				}
			}
			modify_species = {
				species = this
				add_trait = trait_slow_breeders
			}

		}
		if = {
			limit = {
				planet = {
					exists = owner
					owner = { is_ai = no }
					owner = { has_trait = trait_talented }
				}
			}
			modify_species = {
				species = this
				add_trait = trait_talented
			}

		}
		if = {
			limit = {
				planet = {
					exists = owner
					owner = { is_ai = no }
					owner = { has_trait = trait_quick_learners }
				}
			}
			modify_species = {
				species = this
				add_trait = trait_quick_learners
			}

		}
		if = {
			limit = {
				planet = {
					exists = owner
					owner = { is_ai = no }
					owner = { has_trait = trait_slow_learners }
				}
			}
			modify_species = {
				species = this
				add_trait = trait_slow_learners
			}

		}
		if = {
			limit = {
				planet = {
					exists = owner
					owner = { is_ai = no }
					owner = { has_trait = trait_very_strong }
				}
			}
			modify_species = {
				species = this
				add_trait = trait_very_strong
			}

		}
		if = {
			limit = {
				planet = {
					exists = owner
					owner = { is_ai = no }
					owner = { has_trait = trait_strong }
				}
			}
			modify_species = {
				species = this
				add_trait = trait_strong
			}

		}
		if = {
			limit = {
				planet = {
					exists = owner
					owner = { is_ai = no }
					owner = { has_trait = trait_weak }
				}
			}
			modify_species = {
				species = this
				add_trait = trait_weak
			}

		}
		if = {
			limit = {
				planet = {
					exists = owner
					owner = { is_ai = no }
					owner = { has_trait = trait_nomadic }
				}
			}
			modify_species = {
				species = this
				add_trait = trait_nomadic
			}

		}
		if = {
			limit = {
				planet = {
					exists = owner
					owner = { is_ai = no }
					owner = { has_trait = trait_sedentary }
				}
			}
			modify_species = {
				species = this
				add_trait = trait_sedentary
			}

		}
		if = {
			limit = {
				planet = {
					exists = owner
					owner = { is_ai = no }
					owner = { has_trait = trait_communal }
				}
			}
			modify_species = {
				species = this
				add_trait = trait_communal
			}

		}
		if = {
			limit = {
				planet = {
					exists = owner
					owner = { is_ai = no }
					owner = { has_trait = trait_solitary }
				}
			}
			modify_species = {
				species = this
				add_trait = trait_solitary
			}

		}
		if = {
			limit = {
				planet = {
					exists = owner
					owner = { is_ai = no }
					owner = { has_trait = trait_charismatic }
				}
			}
			modify_species = {
				species = this
				add_trait = trait_charismatic
			}

		}
		if = {
			limit = {
				planet = {
					exists = owner
					owner = { is_ai = no }
					owner = { has_trait = trait_repugnant }
				}
			}
			modify_species = {
				species = this
				add_trait = trait_repugnant
			}

		}
		if = {
			limit = {
				planet = {
					exists = owner
					owner = { is_ai = no }
					owner = { has_trait = trait_uplifted }
				}
			}
			modify_species = {
				species = this
				add_trait = trait_uplifted
			}

		}
		if = {
			limit = {
				planet = {
					exists = owner
					owner = { is_ai = no }
					owner = { has_trait = trait_conformists }
				}
			}
			modify_species = {
				species = this
				add_trait = trait_conformists
			}

		}
		if = {
			limit = {
				planet = {
					exists = owner
					owner = { is_ai = no }
					owner = { has_trait = trait_venerable }
				}
			}
			modify_species = {
				species = this
				add_trait = trait_venerable
			}

		}
		if = {
			limit = {
				planet = {
					exists = owner
					owner = { is_ai = no }
					owner = { has_trait = trait_enduring }
				}
			}
			modify_species = {
				species = this
				add_trait = trait_enduring
			}

		}
		if = {
			limit = {
				planet = {
					exists = owner
					owner = { is_ai = no }
					owner = { has_trait = trait_decadent }
				}
			}
			modify_species = {
				species = this
				add_trait = trait_decadent
			}

		}
		if = {
			limit = {
				planet = {
					exists = owner
					owner = { is_ai = no }
					owner = { has_trait = trait_resilient }
				}
			}
			modify_species = {
				species = this
				add_trait = trait_resilient
			}

		}
		if = {
			limit = {
				planet = {
					exists = owner
					owner = { is_ai = no }
					owner = { has_trait = trait_presentient_starborn }
				}
			}
			modify_species = {
				species = this
				add_trait = trait_presentient_starborn
			}

		}
		if = {
			limit = {
				planet = {
					exists = owner
					owner = { is_ai = no }
					owner = { has_trait = trait_presentient_earthbound }
				}
			}
			modify_species = {
				species = this
				add_trait = trait_presentient_earthbound
			}

		}
		if = {
			limit = {
				planet = {
					exists = owner
					owner = { is_ai = no }
					owner = { has_trait = trait_presentient_natural_intellectuals }
				}
			}
			modify_species = {
				species = this
				add_trait = trait_presentient_natural_intellectuals
			}

		}
		if = {
			limit = {
				planet = {
					exists = owner
					owner = { is_ai = no }
					owner = { has_trait = trait_presentient_proles }
				}
			}
			modify_species = {
				species = this
				add_trait = trait_presentient_proles
			}

		}
		if = {
			limit = {
				planet = {
					exists = owner
					owner = { is_ai = no }
					owner = { has_trait = trait_presentient_conservative }
				}
			}
			modify_species = {
				species = this
				add_trait = trait_presentient_conservative
			}

		}
		if = {
			limit = {
				planet = {
					exists = owner
					owner = { is_ai = no }
					owner = { has_trait = trait_presentient_irradiated }
				}
			}
			modify_species = {
				species = this
				add_trait = trait_presentient_irradiated
			}

		}
		#####Extended Traits Begins
		if = {
			limit = {
				planet = {
					exists = owner
					owner = { is_ai = no }
					owner = { has_trait = trait_incompetent_architects }
				}
			}
			modify_species = {
				species = this
				add_trait = trait_incompetent_architects
			}

		}
		if = {
			limit = {
				planet = {
					exists = owner
					owner = { is_ai = no }
					owner = { has_trait = trait_natural_architects }
				}
			}
			modify_species = {
				species = this
				add_trait = trait_natural_architects
			}

		}
		if = {
			limit = {
				planet = {
					exists = owner
					owner = { is_ai = no }
					owner = { has_trait = trait_politically_adept }
				}
			}
			modify_species = {
				species = this
				add_trait = trait_politically_adept
			}

		}
		if = {
			limit = {
				planet = {
					exists = owner
					owner = { is_ai = no }
					owner = { has_trait = trait_virtualists }
				}
			}
			modify_species = {
				species = this
				add_trait = trait_virtualists
			}

		}
		if = {
			limit = {
				planet = {
					exists = owner
					owner = { is_ai = no }
					owner = { has_trait = trait_deliberators }
				}
			}
			modify_species = {
				species = this
				add_trait = trait_deliberators
			}

		}
		if = {
			limit = {
				planet = {
					exists = owner
					owner = { is_ai = no }
					owner = { has_trait = trait_conservationists }
				}
			}
			modify_species = {
				species = this
				add_trait = trait_conservationists
			}

		}
		if = {
			limit = {
				planet = {
					exists = owner
					owner = { is_ai = no }
					owner = { has_trait = trait_urban }
				}
			}
			modify_species = {
				species = this
				add_trait = trait_urban
			}

		}
		if = {
			limit = {
				planet = {
					exists = owner
					owner = { is_ai = no }
					owner = { has_trait = trait_indulgent }
				}
			}
			modify_species = {
				species = this
				add_trait = trait_indulgent
			}

		}
		if = {
			limit = {
				planet = {
					exists = owner
					owner = { is_ai = no }
					owner = { has_trait = trait_content }
				}
			}
			modify_species = {
				species = this
				add_trait = trait_content
			}

		}
		if = {
			limit = {
				planet = {
					exists = owner
					owner = { is_ai = no }
					owner = { has_trait = trait_dim }
				}
			}
			modify_species = {
				species = this
				add_trait = trait_dim
			}

		}
		if = {
			limit = {
				planet = {
					exists = owner
					owner = { is_ai = no }
					owner = { has_trait = trait_inept }
				}
			}
			modify_species = {
				species = this
				add_trait = trait_inept
			}

		}
		if = {
			limit = {
				planet = {
					exists = owner
					owner = { is_ai = no }
					owner = { has_trait = trait_incompetent_engineers }
				}
			}
			modify_species = {
				species = this
				add_trait = trait_incompetent_engineers
			}

		}
		if = {
			limit = {
				planet = {
					exists = owner
					owner = { is_ai = no }
					owner = { has_trait = trait_incompetent_physicists }
				}
			}
			modify_species = {
				species = this
				add_trait = trait_incompetent_physicists
			}

		}
		if = {
			limit = {
				planet = {
					exists = owner
					owner = { is_ai = no }
					owner = { has_trait = trait_incompetent_sociologists }
				}
			}
			modify_species = {
				species = this
				add_trait = trait_incompetent_sociologists
			}

		}
		if = {
			limit = {
				planet = {
					exists = owner
					owner = { is_ai = no }
					owner = { has_trait = trait_very_weak }
				}
			}
			modify_species = {
				species = this
				add_trait = trait_very_weak
			}

		}
		if = {
			limit = {
				planet = {
					exists = owner
					owner = { is_ai = no }
					owner = { has_trait = trait_valiant }
				}
			}
			modify_species = {
				species = this
				add_trait = trait_valiant
			}

		}
		if = {
			limit = {
				planet = {
					exists = owner
					owner = { is_ai = no }
					owner = { has_trait = trait_craven }
				}
			}
			modify_species = {
				species = this
				add_trait = trait_craven
			}

		}
		if = {
			limit = {
				planet = {
					exists = owner
					owner = { is_ai = no }
					owner = { has_trait = trait_individualistic }
				}
			}
			modify_species = {
				species = this
				add_trait = trait_individualistic
			}

		}
		if = {
			limit = {
				planet = {
					exists = owner
					owner = { is_ai = no }
					owner = { has_trait = trait_transient }
				}
			}
			modify_species = {
				species = this
				add_trait = trait_transient
			}

		}
		if = {
			limit = {
				planet = {
					exists = owner
					owner = { is_ai = no }
					owner = { has_trait = trait_resourceful }
				}
			}
			modify_species = {
				species = this
				add_trait = trait_resourceful
			}

		}
		if = {
			limit = {
				planet = {
					exists = owner
					owner = { is_ai = no }
					owner = { has_trait = trait_delicate }
				}
			}
			modify_species = {
				species = this
				add_trait = trait_delicate
			}

		}
		#####Extended Traits Ends
		if = {
			limit = {
				planet = {
					exists = owner
					owner = { is_ai = no }
					owner = { has_trait = trait_pc_desert_preference }
				}
			}
			modify_species = {
				species = this
				add_trait = trait_pc_desert_preference
			}

		}
		if = {
			limit = {
				planet = {
					exists = owner
					owner = { is_ai = no }
					owner = { has_trait = trait_pc_tropical_preference }
				}
			}
			modify_species = {
				species = this
				add_trait = trait_pc_tropical_preference
			}

		}
		if = {
			limit = {
				planet = {
					exists = owner
					owner = { is_ai = no }
					owner = { has_trait = trait_pc_continental_preference }
				}
			}
			modify_species = {
				species = this
				add_trait = trait_pc_continental_preference
			}

		}
		if = {
			limit = {
				planet = {
					exists = owner
					owner = { is_ai = no }
					owner = { has_trait = trait_pc_ocean_preference }
				}
			}
			modify_species = {
				species = this
				add_trait = trait_pc_ocean_preference
			}

		}
		if = {
			limit = {
				planet = {
					exists = owner
					owner = { is_ai = no }
					owner = { has_trait = trait_pc_arctic_preference }
				}
			}
			modify_species = {
				species = this
				add_trait = trait_pc_arctic_preference
			}

		}
		if = {
			limit = {
				planet = {
					exists = owner
					owner = { is_ai = no }
					owner = { has_trait = trait_pc_tundra_preference }
				}
			}
			modify_species = {
				species = this
				add_trait = trait_pc_tundra_preference
			}

		}
		if = {
			limit = {
				planet = {
					exists = owner
					owner = { is_ai = no }
					owner = { has_trait = trait_pc_arid_preference }
				}
			}
			modify_species = {
				species = this
				add_trait = trait_pc_arid_preference
			}

		}
		if = {
			limit = {
				planet = {
					exists = owner
					owner = { is_ai = no }
					owner = { has_trait = trait_pc_ai_preference }
				}
			}
			modify_species = {
				species = this
				add_trait = trait_pc_ai_preference
			}

		}
		if = {
			limit = {
				planet = {
					exists = owner
					owner = { is_ai = no }
					owner = { has_trait = trait_pc_nuked_preference }
				}
			}
			modify_species = {
				species = this
				add_trait = trait_pc_nuked_preference
			}

		}
		if = {
			limit = {
				planet = {
					exists = owner
					owner = { is_ai = no }
					owner = { has_trait = trait_pc_gaia_preference }
				}
			}
			modify_species = {
				species = this
				add_trait = trait_pc_gaia_preference
			}

		}
		if = {
			limit = {
				planet = {
					exists = owner
					owner = { is_ai = no }
					owner = { has_trait = trait_pc_ring_preference }
				}
			}
			modify_species = {
				species = this
				add_trait = trait_pc_ring_preference
			}

		}
	}
}

pop_event = {
	id = playablerobots.3
	title = playablerobots.2.title
	desc = tutorial.14.desc
	picture = GFX_evt_playablerobots
	hide_window = yes
	#is_triggered_only = yes

	trigger = {
		#is_ai = no
		#planet = {
		#	any_pop = {
		#		AND = {
					has_trait = trait_android
					is_same_species = owner
					AND = {
						NOT = {has_trait = trait_android_level1}
						NOT = {has_trait = trait_android_level2}
						NOT = {has_trait = trait_android_level3}
					}
					planet = {
						exists = owner
						owner = { is_ai = no }
						owner = { has_technology  = tech_robotic_workers }
						owner = { NOT = { has_technology  = tech_droid_workers } }
					}
					#any_country = {
					#	is_ai = no
					#	has_technology  = tech_robotic_workers
					#	NOT = { has_technology  = tech_droid_workers }
					#}
		#		}
		#	}
		#}
	}

	mean_time_to_happen = { days = 1 }

	immediate = {
	}

	option = {
		name = OK
		modify_species = {
			species = this
			add_trait = trait_android_level1
		}
	}
}

pop_event = {
	id = playablerobots.4
	title = playablerobots.2.title
	desc = tutorial.14.desc
	picture = GFX_evt_playablerobots
	hide_window = yes
	#is_triggered_only = yes

	trigger = {
		#is_ai = no
		#planet = {
		#	any_pop = {
		#		AND = {
					has_trait = trait_android
					#is_same_species = owner
					AND = {
						NOT = {has_trait = trait_android_level2}
						NOT = {has_trait = trait_android_level3}
					}
					planet = {
						exists = owner
						owner = { is_ai = no }
						owner = { has_technology  = tech_droid_workers }
						owner = { NOT = { has_technology  = tech_synthetic_workers } }
					}
					#any_country = {
					#	is_ai = no
					#	has_technology  = tech_droid_workers
					#	NOT = { has_technology  = tech_synthetic_workers }
					#}
		#		}
		#	}
		#}
	}

	mean_time_to_happen = { days = 1 }

	immediate = {
	}

	option = {
		name = OK
		modify_species = {
			species = this
			add_trait = trait_android_level2
			remove_trait = trait_android_level1
		}
	}
}

pop_event = {
	id = playablerobots.5
	title = playablerobots.2.title
	desc = tutorial.14.desc
	picture = GFX_evt_playablerobots
	hide_window = yes
	#is_triggered_only = yes

	trigger = {
		#is_ai = no
		#planet = {
		#	any_pop = {
		#		AND = {
					has_trait = trait_android
					#is_same_species = owner
					AND = {
						NOT = {has_trait = trait_android_level3}
					}
					planet = {
						exists = owner
						owner = { is_ai = no }
						owner = { has_technology  = tech_synthetic_workers }
					}
					#any_country = {
					#	is_ai = no
					#	has_technology  = tech_synthetic_workers
					#}
		#		}
		#	}
		#}
	}

	mean_time_to_happen = { days = 1 }

	immediate = {
	}

	option = {
		name = OK
		modify_species = {
			species = this
			add_trait = trait_android_level3
			remove_trait = trait_android_level2
			remove_trait = trait_android_level1
		}
	}
}

country_event = {
	id = playablerobots.6
	title = playablerobots.2.title
	desc = tutorial.14.desc
	picture = GFX_evt_playablerobots
	hide_window = yes
	#is_triggered_only = yes

	trigger = {
		any_owned_pop = {
					has_trait = trait_android
					NOT = {is_growing = yes}
					NOT = {is_same_species = owner}
		}
		is_ai = no
	}

	mean_time_to_happen = { days = 1 }

	immediate = {
	}

	option = {
		name = OK
		every_owned_planet = {
			every_owned_pop = {
			limit = {
					has_trait = trait_android
					NOT = {is_growing = yes}
					NOT = {is_same_species = owner}
			}
				change_pop_species = ROOT
			}
		}
	}
}

country_event = {
	id = playablerobots.7
	title = playablerobots.2.title
	desc = tutorial.14.desc
	picture = GFX_evt_playablerobots
	hide_window = yes
	#is_triggered_only = yes

	trigger = {
		sectors > 0
		has_country_edict  = playablerobots_sector
		is_ai = no
		has_country_resource = {
			type = minerals
			amount > 100
		}
		has_country_resource = {
			type = energy
			amount > 250
		}
		any_owned_planet = {
			#limit = {
			#	NOT = { sector_controlled = no }
			#}
			any_tile = {
				AND = {
						has_grown_pop = no
						has_growing_pop = no
					 	has_blocker = no
					}
			}
		}
	}

	mean_time_to_happen = { days = 350 }

	#immediate = {
	#}

	option = {
		name = OK
		random_owned_planet = {
			limit = {
					sector_controlled = yes
			}
			if = {
				limit = {
					owner = {
						NOT = { has_technology  = tech_robotic_workers }
					}
				}
				owner = {
					add_minerals = -30
					add_energy = -100
				}
			}
			if = {
				limit = {
					owner = {
						has_technology  = tech_robotic_workers
						NOT = { has_technology  = tech_droid_workers }
					}
				}
				owner = {
					add_minerals = -40
					add_energy = -125
				}
			}
			if = {
				limit = {
					owner = {
						has_technology  = tech_droid_workers
						NOT = { has_technology  = tech_synthetic_workers }
					}
				}
				owner = {
					add_minerals = -50
					add_energy = -150
				}
			}
			if = {
				limit = {
					owner = {
						has_technology  = tech_synthetic_workers
					}
				}
				owner = {
					add_minerals = -60
					add_energy = -175
				}
			}
			best_tile_for_pop = {
				create_pop = {
					species = owner_main_species
					ethos = owner
				}
			}
		}
	}
}

country_event = {
	id = playablerobots.8
	title = playablerobots.2.title
	desc = tutorial.14.desc
	picture = GFX_evt_playablerobots
	hide_window = yes
	#is_triggered_only = yes

	trigger = {
		has_trait = trait_android
		has_country_edict  = playablerobots_immortal
		is_ai = no
		NOT = { has_modifier = playablerobots_immortal_modifier }
	}

	mean_time_to_happen = { days = 1 }

	#immediate = {
	#}

	option = {
		name = OK
		add_modifier = {
			modifier = "playablerobots_immortal_modifier"
			days = -1
		}
	}
}

country_event = {
	id = playablerobots.9
	title = playablerobots.2.title
	desc = tutorial.14.desc
	picture = GFX_evt_playablerobots
	hide_window = yes
	#is_triggered_only = yes

	trigger = {
		has_trait = trait_android
		NOT = { has_country_edict  = playablerobots_immortal }
		is_ai = no
		has_modifier = playablerobots_immortal_modifier
	}

	mean_time_to_happen = { days = 1 }

	#immediate = {
	#}

	option = {
		name = OK
		remove_modifier = playablerobots_immortal_modifier
	}
}

country_event = {
	id = playablerobots.10
	title = playablerobots.2.title
	desc = tutorial.14.desc
	picture = GFX_evt_playablerobots
	hide_window = yes
	#is_triggered_only = yes

	trigger = {
		OR = {
			has_government = technocratic_confederacy
			has_government = technocratic_hegemony
		}
		is_ai = yes
	}

	mean_time_to_happen = { days = 1 }

	#immediate = {
	#}

	option = {
		name = OK
		ai_chance = {
			factor = 33
		}
		change_government = military_republic
	}

	option = {
		name = OK
		ai_chance = {
			factor = 33
		}
		change_government = theocratic_republic
	}

	option = {
		name = OK
		ai_chance = {
			factor = 33
		}
		change_government = direct_democracy
	}

	option = {
		name = OK
		ai_chance = {
			factor = 33
		}
		change_government = moral_democracy
	}

	option = {
		name = OK
		ai_chance = {
			factor = 33
		}
		change_government = indirect_democracy
	}
}

country_event = {
	id = playablerobots.14
	title = playablerobots.2.title
	desc = tutorial.14.desc
	picture = GFX_evt_playablerobots
	hide_window = yes
	#is_triggered_only = yes

	trigger = {
		OR = {
			has_government = neural_cooperative
			has_government = neural_collective
		}
		is_ai = yes
	}

	mean_time_to_happen = { days = 1 }

	#immediate = {
	#}

	option = {
		name = OK
		ai_chance = {
			factor = 33
		}
		change_government = military_junta
	}

	option = {
		name = OK
		ai_chance = {
			factor = 33
		}
		change_government = theocratic_oligarchy
	}

	option = {
		name = OK
		ai_chance = {
			factor = 33
		}
		change_government = science_directorate
	}

	option = {
		name = OK
		ai_chance = {
			factor = 33
		}
		change_government = peaceful_bureaucracy
	}

	option = {
		name = OK
		ai_chance = {
			factor = 33
		}
		change_government = plutocratic_oligarchy
	}
}

country_event = {
	id = playablerobots.15
	title = playablerobots.2.title
	desc = tutorial.14.desc
	picture = GFX_evt_playablerobots
	hide_window = yes
	#is_triggered_only = yes

	trigger = {
		OR = {
			has_government = collective_consciousness
			has_government = collective_intelligence
		}
		is_ai = yes
	}

	mean_time_to_happen = { days = 1 }

	#immediate = {
	#}

	option = {
		name = OK
		ai_chance = {
			factor = 33
		}
		change_government = military_dictatorship
	}

	option = {
		name = OK
		ai_chance = {
			factor = 33
		}
		change_government = divine_mandate
	}

	option = {
		name = OK
		ai_chance = {
			factor = 33
		}
		change_government = despotic_hegemony
	}

	option = {
		name = OK
		ai_chance = {
			factor = 33
		}
		change_government = enlightened_monarchy
	}

	option = {
		name = OK
		ai_chance = {
			factor = 33
		}
		change_government = despotic_empire
	}
}
